.row
  .col-md-12
    .ibox
      .ibox-title
        My Account
      .ibox-content
        = form_with(model: resource, as: resource_name, url: registration_path(resource_name), class: "m-t", local: true) do |f|
          = devise_error_messages!
          .form-group.row
            = f.label :avatar, class: "col-md-2 col-form-label"
            .col-md-10
              .row
                .col-md-3
                  = f.file_field :avatar
                .col-md-9
                  - if resource.avatar.attached?
                    = image_tag resource.avatar.variant(resize: "60x60")
          .form-group.row
            = f.label :first_name, class: "col-md-2 col-form-label"
            .col-md-10
              = f.text_field :first_name, class: "form-control", autofocus: true, required: true, placeholder: "First Name"
          .form-group.row
            = f.label :last_name, class: "col-md-2 col-form-label"
            .col-md-10
              = f.text_field :last_name, class: "form-control", required: true, placeholder: "Last Name"
          .form-group.row
            = f.label :email, class: "col-md-2 col-form-label"
            .col-md-10
              = f.email_field :email, class: "form-control", required: true, placeholder: "Email"
          .form-group.row
            = f.label :password, class: "col-md-2 col-form-label"
            .col-md-10
              = f.password_field :password, class: "form-control", placeholder: "Password"
          .form-group.row
            = f.label :password_confirmation, class: "col-md-2 col-form-label"
            .col-md-10
              = f.password_field :password_confirmation, class: "form-control", placeholder: "Password Confirmation"
          .form-group.row
            = f.label :current_password, class: "col-md-2 col-form-label"
            .col-md-10
              = f.password_field :current_password, class: "form-control", required: true, placeholder: "Current Password"
              %span.form-text.m-b-none You need to enter you current password in order to edit your profile
          = f.submit "Update", class: "btn btn-primary block m-b"
