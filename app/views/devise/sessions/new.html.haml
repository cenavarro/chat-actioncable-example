.middle-box.text-center.loginscreen.animated.fadeInDown
  %div
    %h1.logo-name Chat
  %h3 Welcome to Chat App Example
  %p Login in. To see it in action.
  = form_with(model: resource, as: resource_name, url: session_path(resource_name), class: "m-t", local: true) do |f|
    .form-group
      = f.email_field :email, autofocus: true, class: "form-control", required: true, placeholder: "Email"
    .form-group
      = f.password_field :password, class: "form-control", required: true, placeholder: "Password"
    - if devise_mapping.rememberable?
      .form-group
        = f.check_box :remember_me
        = f.label :remember_me
    = f.submit "Login", class: "btn btn-primary block full-width m-b"
  %p.text-muted.text-center
    %small Do not have an account?
  = link_to "Create an account", new_registration_path(resource_name), class: "btn btn-white btn-block"
